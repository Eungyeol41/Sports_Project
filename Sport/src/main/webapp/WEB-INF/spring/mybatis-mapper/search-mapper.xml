<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.sport.dao.SearchDao">

	<select id="selectAllList" resultType="AllListVO">
		SELECT * FROM tbl_all_list
	</select>
	
	<select id="findFree" resultType="AllListVO">
		SELECT * FROM tbl_all_list
		WHERE al_free = #{al_free} AND al_name like CONCAT('%',#{al_name},'%') AND al_sport = #{al_sport}
		ORDER BY al_seq DESC
		
	</select>

	<select id="findBySports" resultType="AllListVO">
	    SELECT * FROM tbl_all_list
	    WHERE al_sport like CONCAT('%',#{keyword},'%')
    </select>


	<insert id="insert"></insert>
	<update id="update"></update>
	<delete id="delete"></delete>

</mapper> 